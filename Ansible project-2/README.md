# Deployment of simple webapp using ansible,
# with the use of variable
# created three variables
   db_name
   db_user
   db_password

# Inorder to use these variable put it inside {{ variable name }}